import React, { forwardRef } from 'react'

const SnackbarHandler = forwardRef((props, ref) => {
  return <div>todo</div>
})

SnackbarHandler.displayName = 'Actify.SnackbarHandler'

export default SnackbarHandler
