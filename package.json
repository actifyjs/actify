{
  "name": "actify",
  "private": true,
  "scripts": {
    "prebuild": "node ./scripts/prebuild.js",
    "build": "npm run prebuild && turbo run build",
    "predev": "node ./scripts/predev.js",
    "dev": "npm run predev && turbo run dev --no-cache --continue"
  },
  "dependencies": {
    "@changesets/cli": "^2.27.1",
    "@manypkg/cli": "^0.21.1",
    "eslint": "^8.56.0",
    "prettier": "^3.1.1",
    "turbo": "^1.11.2"
  },
  "packageManager": "pnpm@8.11.0"
}
